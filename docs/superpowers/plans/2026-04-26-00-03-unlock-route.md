# 00-03 Unlock Route Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a `foundations/` unlock layer that lets learners follow the Transformer mainline first, then use short bridge pages to unlock old `00-03` materials at the right time.

**Implementation status:** Completed and verified in branch `00-03-unlock-route`. The checklist below is the original execution plan; see branch commits for the implemented task sequence.

**Architecture:** Add two new foundation groups, `math-for-transformer` and `nlp-tokenization-embedding`, plus a root `foundations/README.md`. Update Transformer prerequisite links to point to the new bridge pages first, and update old `00-03` README files so they are clearly positioned as unlocked deep-dive references rather than the default route.

**Tech Stack:** Markdown documentation, existing `ai/ml-to-llm-roadmap` structure, shell verification with `rg`, `git diff --check`, and local Markdown link/heading checkers.

---

## Scope

Included:

1. `foundations/README.md` as the new unlock-route entry.
2. `foundations/math-for-transformer/` with three bridge pages.
3. `foundations/nlp-tokenization-embedding/` with three bridge pages.
4. Small navigation updates to existing `foundations/deep-learning/`.
5. Navigation updates in root roadmap, Transformer mainline, and old `00-03` README files.
6. Verification that links, required headings, and route-positioning language are coherent.

Not included:

1. Rewriting old `00-03` article bodies wholesale.
2. Creating interview cheatsheets.
3. Adding second-batch topics like cross entropy, perplexity, optimizer details, or full BPE algorithm derivations.
4. Changing RAG, Agent, training, inference, evaluation, or system design module content beyond links that directly support this unlock route.

## File Structure

Create:

- `ai/ml-to-llm-roadmap/foundations/README.md`
- `ai/ml-to-llm-roadmap/foundations/math-for-transformer/README.md`
- `ai/ml-to-llm-roadmap/foundations/math-for-transformer/01-vector-matrix-dot-product.md`
- `ai/ml-to-llm-roadmap/foundations/math-for-transformer/02-logits-softmax-probability.md`
- `ai/ml-to-llm-roadmap/foundations/math-for-transformer/03-attention-math-minimal.md`
- `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/README.md`
- `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/01-token-vocab-token-id.md`
- `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/02-embedding-table-and-position.md`
- `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/03-from-text-representation-to-transformer.md`

Modify:

- `ai/ml-to-llm-roadmap.md`
- `ai/ml-to-llm-roadmap/04-transformer-foundations/README.md`
- `ai/ml-to-llm-roadmap/04-transformer-foundations/02-token-to-vector.md`
- `ai/ml-to-llm-roadmap/04-transformer-foundations/03-why-attention-needs-context.md`
- `ai/ml-to-llm-roadmap/04-transformer-foundations/04-self-attention-qkv.md`
- `ai/ml-to-llm-roadmap/04-transformer-foundations/05-transformer-block.md`
- `ai/ml-to-llm-roadmap/foundations/deep-learning/README.md`
- `ai/ml-to-llm-roadmap/foundations/deep-learning/01-neuron-mlp-activation.md`
- `ai/ml-to-llm-roadmap/foundations/deep-learning/02-backprop-gradient-problems.md`
- `ai/ml-to-llm-roadmap/foundations/deep-learning/03-normalization-residual-initialization.md`
- `ai/ml-to-llm-roadmap/foundations/deep-learning/04-ffn-gating-for-transformer.md`
- `ai/ml-to-llm-roadmap/00-math-foundations/README.md`
- `ai/ml-to-llm-roadmap/01-ml-basics/README.md`
- `ai/ml-to-llm-roadmap/02-deep-learning/README.md`
- `ai/ml-to-llm-roadmap/03-nlp-embedding-retrieval/README.md`

## New Foundation Page Standard

Every new foundation article must use this exact heading order:

```markdown
## 这篇解决什么卡点
## 先记住一句话
## 最小例子
## 这个概念在 Transformer 哪里出现
## 和旧资料的连接
## 自测
## 回到主线
```

Rules:

- Explain the learning blocker first, then the term.
- Include one concrete numeric or text example.
- Do not do full mathematical derivations.
- Link back to the Transformer page that caused the card.
- Link old `00-03` pages only as deep-dive references near the end.
- Keep the page short enough to be used as a bridge, not a replacement course.

---

## Task 1: Add Foundations Root Entry

**Files:**

- Create: `ai/ml-to-llm-roadmap/foundations/README.md`

- [ ] **Step 1: Create root foundations directory entry**

Create `ai/ml-to-llm-roadmap/foundations/README.md` with this structure:

```markdown
# Foundations 解锁层

> **定位**：这里不是默认主线，也不是旧 `00-03` 的替代品。默认先读 [Transformer 必要基础](../04-transformer-foundations/)；卡在数学、NLP 或神经网络前置概念时，再回到这里补一小块，补完立刻回主线。

## 什么时候来这里

| 你卡在哪里 | 先读 |
|------------|------|
| 向量、矩阵、点积、`QK^T` | [Transformer 最小数学](./math-for-transformer/) |
| logits、softmax、概率分布 | [Transformer 最小数学](./math-for-transformer/) |
| token、vocab、token id、embedding table | [Tokenization 与 Embedding 补课](./nlp-tokenization-embedding/) |
| 神经元、线性层、MLP、激活 | [Deep Learning 补课](./deep-learning/) |
| Residual、LayerNorm、FFN、GELU/SwiGLU | [Deep Learning 补课](./deep-learning/) |

## 推荐读法

1. 先读 [Transformer 必要基础](../04-transformer-foundations/)。
2. 只在主线卡住时进入对应 foundation 小文档。
3. 读完小文档后回到原 Transformer 章节。
4. 主线跑通后，再回读旧 `00-03` 深入资料。

## 和旧 `00-03` 的关系

旧 `00-03` 仍然有价值，但它们是解锁后的深入资料：

- [00 数学基础](../00-math-foundations/)：读完最小数学后，用来补线性代数、概率、微积分、信息论的完整直觉。
- [01 机器学习基础](../01-ml-basics/)：读完主线后，用来补训练、评估、泛化和传统 ML 语境。
- [02 深度学习基础](../02-deep-learning/)：读完 Deep Learning 补课后，用来补 CNN/RNN/Attention 演进脉络。
- [03 NLP、Embedding 与检索理论](../03-nlp-embedding-retrieval/)：读完 token/embedding 补课后，用来补文本表示、embedding 和检索理论。

## 不要怎么读

- 不要从这里横向扩散，把所有 foundation 一次性读完。
- 不要把旧 `00-03` 当成进入 Transformer 的硬性前置。
- 不要在第一次学习时追求公式推导完整性。

> 回到主线：[Transformer 必要基础](../04-transformer-foundations/)
```

- [ ] **Step 2: Verify root entry**

Run:

```bash
rg -n "Foundations 解锁层|什么时候来这里|和旧 `00-03` 的关系|不要怎么读|math-for-transformer|nlp-tokenization-embedding|deep-learning" ai/ml-to-llm-roadmap/foundations/README.md
git diff --check
```

Expected:

- All key route labels are present.
- `git diff --check` has no output.

- [ ] **Step 3: Commit root entry**

Run:

```bash
git add ai/ml-to-llm-roadmap/foundations/README.md
git commit -m "docs: add foundations unlock entry"
```

---

## Task 2: Add Math For Transformer Foundations

**Files:**

- Create: `ai/ml-to-llm-roadmap/foundations/math-for-transformer/README.md`
- Create: `ai/ml-to-llm-roadmap/foundations/math-for-transformer/01-vector-matrix-dot-product.md`
- Create: `ai/ml-to-llm-roadmap/foundations/math-for-transformer/02-logits-softmax-probability.md`
- Create: `ai/ml-to-llm-roadmap/foundations/math-for-transformer/03-attention-math-minimal.md`

- [ ] **Step 1: Create math directory**

Run:

```bash
mkdir -p ai/ml-to-llm-roadmap/foundations/math-for-transformer
```

Expected:

- Directory exists.

- [ ] **Step 2: Create math README**

Create `ai/ml-to-llm-roadmap/foundations/math-for-transformer/README.md` with:

```markdown
# Transformer 最小数学

> **定位**：这里不是完整数学课，只解决读 Transformer 时最容易卡住的几个数学动作：向量、矩阵、点积、logits、softmax 和 Attention 公式。

## 默认学习顺序

1. [向量、矩阵和点积](./01-vector-matrix-dot-product.md)
2. [Logits、Softmax 与概率分布](./02-logits-softmax-probability.md)
3. [Attention 公式的最小数学拆解](./03-attention-math-minimal.md)

## 什么时候读

| 你在主线哪里卡住 | 先读 |
|------------------|------|
| `02-token-to-vector` 里不理解向量和矩阵 | [向量、矩阵和点积](./01-vector-matrix-dot-product.md) |
| `04-self-attention-qkv` 里不理解 `QK^T` | [向量、矩阵和点积](./01-vector-matrix-dot-product.md) |
| 不理解 softmax 为什么输出权重 | [Logits、Softmax 与概率分布](./02-logits-softmax-probability.md) |
| 看见 `softmax(QK^T / sqrt(d_k))V` 就断掉 | [Attention 公式的最小数学拆解](./03-attention-math-minimal.md) |

## 和旧数学资料的关系

读完这里后，如果想补完整数学直觉，再回到：

- [旧版线性代数基础](../../00-math-foundations/01-linear-algebra.md)
- [旧版概率基础](../../00-math-foundations/02-probability.md)

微积分和信息论可以等到训练、损失函数、KL/RLHF 等主题时再读。

> 回到主线：[Transformer 必要基础](../../04-transformer-foundations/)
```

- [ ] **Step 3: Create `01-vector-matrix-dot-product.md`**

Create the page using the New Foundation Page Standard. Required content:

- Explain that a vector is a list of numbers used to represent an object.
- Explain that a matrix can transform one vector or many vectors.
- Explain dot product as a simple matching score between two vectors.
- Use a numeric example:

```text
q = [1, 0]
k1 = [1, 0] -> q · k1 = 1
k2 = [0, 1] -> q · k2 = 0
```

- Connect to:
  - token embeddings in `../../04-transformer-foundations/02-token-to-vector.md`
  - Q/K matching in `../../04-transformer-foundations/04-self-attention-qkv.md`
  - old linear algebra in `../../00-math-foundations/01-linear-algebra.md`
- Include self-check questions:
  1. 向量为什么可以表示 token？
  2. 点积为什么可以当作匹配分数？
  3. `QK^T` 为什么会得到一个 token 对 token 的分数矩阵？

- [ ] **Step 4: Create `02-logits-softmax-probability.md`**

Create the page using the New Foundation Page Standard. Required content:

- Explain logits as raw scores before normalization.
- Explain softmax as turning scores into non-negative weights that sum to 1.
- Use a numeric example:

```text
logits = [2, 1, 0]
softmax 后大致变成 [0.67, 0.24, 0.09]
```

- Explain that bigger score gets bigger probability, but all candidates still keep some weight.
- Explain temperature only as distribution sharpness:
  - lower temperature -> sharper, more conservative
  - higher temperature -> flatter, more diverse
- Connect to:
  - attention weights in `../../04-transformer-foundations/04-self-attention-qkv.md`
  - next-token probability in `../../04-transformer-foundations/08-decoder-only-generation.md`
  - old probability in `../../00-math-foundations/02-probability.md`
- Include self-check questions:
  1. logits 和 probability 的区别是什么？
  2. softmax 为什么适合表示“读取比例”？
  3. temperature 改变的是知识还是分布形状？

- [ ] **Step 5: Create `03-attention-math-minimal.md`**

Create the page using the New Foundation Page Standard. Required content:

- Break `softmax(QK^T / sqrt(d_k))V` into four steps:
  1. `QK^T` computes matching scores.
  2. `/ sqrt(d_k)` keeps score scale stable.
  3. `softmax` turns scores into read weights.
  4. multiplying by `V` mixes content vectors.
- Use a small text example:

```text
query token: "苹果"
context tokens: ["发布", "吃"]
如果 "苹果" 更像公司，attention 可能更看 "发布"。
如果 "苹果" 更像水果，attention 可能更看 "吃"。
```

- Explain every row of attention weights as “one token deciding how much to read from all tokens”.
- Connect to:
  - attention motivation in `../../04-transformer-foundations/03-why-attention-needs-context.md`
  - Q/K/V page in `../../04-transformer-foundations/04-self-attention-qkv.md`
  - old linear algebra and probability pages
- Include self-check questions:
  1. Attention scores 和 attention weights 有什么区别？
  2. 为什么每一行权重和为 1？
  3. 为什么乘以 `V` 才得到输出内容？

- [ ] **Step 6: Verify and commit math foundations**

Run:

```bash
rg -n "这篇解决什么卡点|先记住一句话|最小例子|这个概念在 Transformer 哪里出现|和旧资料的连接|自测|回到主线|QK\\^T|softmax|点积|logits" ai/ml-to-llm-roadmap/foundations/math-for-transformer
git diff --check
```

Commit:

```bash
git add ai/ml-to-llm-roadmap/foundations/math-for-transformer
git commit -m "docs: add transformer math foundations"
```

---

## Task 3: Add NLP Tokenization And Embedding Foundations

**Files:**

- Create: `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/README.md`
- Create: `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/01-token-vocab-token-id.md`
- Create: `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/02-embedding-table-and-position.md`
- Create: `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/03-from-text-representation-to-transformer.md`

- [ ] **Step 1: Create NLP foundation directory**

Run:

```bash
mkdir -p ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding
```

Expected:

- Directory exists.

- [ ] **Step 2: Create NLP foundation README**

Create `ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/README.md` with:

```markdown
# Tokenization 与 Embedding 补课

> **定位**：这里只解决文本进入 Transformer 前的最小前置：token、vocab、token id、embedding table 和 position。完整 NLP 表示史留给旧 `03-nlp-embedding-retrieval` 深入阅读。

## 默认学习顺序

1. [Token、Vocab 与 Token ID](./01-token-vocab-token-id.md)
2. [Embedding Table 与位置信息](./02-embedding-table-and-position.md)
3. [从文本表示演进到 Transformer 输入](./03-from-text-representation-to-transformer.md)

## 什么时候读

| 你在主线哪里卡住 | 先读 |
|------------------|------|
| 不理解 token 为什么不是字或词 | [Token、Vocab 与 Token ID](./01-token-vocab-token-id.md) |
| 不理解 token id 为什么没有语义 | [Token、Vocab 与 Token ID](./01-token-vocab-token-id.md) |
| 不理解 embedding table | [Embedding Table 与位置信息](./02-embedding-table-and-position.md) |
| 不理解为什么还要 position | [Embedding Table 与位置信息](./02-embedding-table-and-position.md) |
| 想知道旧 NLP 文本表示和 Transformer 的关系 | [从文本表示演进到 Transformer 输入](./03-from-text-representation-to-transformer.md) |

## 和旧 NLP 资料的关系

读完这里后，如果想补完整背景，再回到：

- [旧版文本表示演进](../../03-nlp-embedding-retrieval/01-text-representation.md)
- [旧版 Embedding 理论](../../03-nlp-embedding-retrieval/02-embedding-theory.md)
- [旧版 Tokenization 深入](../../03-nlp-embedding-retrieval/06-tokenization-deep-dive.md)

> 回到主线：[从 Token 到向量](../../04-transformer-foundations/02-token-to-vector.md)
```

- [ ] **Step 3: Create `01-token-vocab-token-id.md`**

Create the page using the New Foundation Page Standard. Required content:

- Explain token as model-side text unit, not always a word.
- Explain vocab as the model’s allowed token inventory.
- Explain token id as an integer index into vocab.
- Use a concrete example:

```text
"unhappy" 可能被切成 ["un", "happy"]
"user_id" 在代码或 JSON 里也会被拆成多个 token
```

- Explain that token id is a number label, not semantic meaning.
- Connect to:
  - `../../04-transformer-foundations/02-token-to-vector.md`
  - old tokenization deep dive `../../03-nlp-embedding-retrieval/06-tokenization-deep-dive.md`
- Include self-check questions:
  1. token 为什么不一定等于词？
  2. vocab 和 token id 的关系是什么？
  3. 为什么 token id 本身没有语义？

- [ ] **Step 4: Create `02-embedding-table-and-position.md`**

Create the page using the New Foundation Page Standard. Required content:

- Explain embedding table as a matrix with shape `vocab_size x d_model`.
- Explain lookup:

```text
token id = 314
embedding table[314] -> 这个 token 的向量
```

- Explain token embedding only gives content identity, not order.
- Explain position information as how Transformer distinguishes “A before B” from “B before A”.
- Keep position explanation generic: position embedding, RoPE, or other position mechanisms all solve order information.
- Connect to:
  - `../../04-transformer-foundations/02-token-to-vector.md`
  - old embedding theory `../../03-nlp-embedding-retrieval/02-embedding-theory.md`
- Include self-check questions:
  1. embedding table 的行数和列数分别代表什么？
  2. 为什么查表后得到的是向量而不是文字？
  3. 为什么只靠 token embedding 不知道顺序？

- [ ] **Step 5: Create `03-from-text-representation-to-transformer.md`**

Create the page using the New Foundation Page Standard. Required content:

- Explain the problem progression:

```text
One-hot/BoW: 能编号和统计，但语义弱
Word2Vec: 静态词向量，能表达相似性
Contextual embedding: 同一个词在不同上下文里表示不同
Transformer hidden state: token 向量逐层吸收上下文
```

- Emphasize this is only the bridge; it does not replace old NLP history.
- Connect to:
  - context page `../../04-transformer-foundations/03-why-attention-needs-context.md`
  - old text representation `../../03-nlp-embedding-retrieval/01-text-representation.md`
  - old embedding theory `../../03-nlp-embedding-retrieval/02-embedding-theory.md`
- Include self-check questions:
  1. One-hot 为什么不能表达语义相近？
  2. 静态 embedding 和 contextual embedding 的区别是什么？
  3. Transformer hidden state 为什么不是最初的 token embedding？

- [ ] **Step 6: Verify and commit NLP foundations**

Run:

```bash
rg -n "这篇解决什么卡点|先记住一句话|最小例子|这个概念在 Transformer 哪里出现|和旧资料的连接|自测|回到主线|token id|embedding table|position|One-hot|contextual" ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding
git diff --check
```

Commit:

```bash
git add ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding
git commit -m "docs: add tokenization embedding foundations"
```

---

## Task 4: Integrate Existing Deep Learning Foundations

**Files:**

- Modify: `ai/ml-to-llm-roadmap/foundations/deep-learning/README.md`
- Modify: `ai/ml-to-llm-roadmap/foundations/deep-learning/01-neuron-mlp-activation.md`
- Modify: `ai/ml-to-llm-roadmap/foundations/deep-learning/02-backprop-gradient-problems.md`
- Modify: `ai/ml-to-llm-roadmap/foundations/deep-learning/03-normalization-residual-initialization.md`
- Modify: `ai/ml-to-llm-roadmap/foundations/deep-learning/04-ffn-gating-for-transformer.md`

- [ ] **Step 1: Update Deep Learning foundation README**

Modify `ai/ml-to-llm-roadmap/foundations/deep-learning/README.md`:

- Add a line near the top:

```markdown
> 总补课入口：[Foundations 解锁层](../README.md)
```

- Keep the existing positioning that it is a补课层, not default route.
- Ensure the “推荐读法” says:
  1. default first read Transformer mainline;
  2. enter only the page matching the current blocker;
  3. return to the original Transformer section;
  4. use old `02-deep-learning/` only as deep reference.

- [ ] **Step 2: Check return links in existing DL pages**

Inspect each existing page and ensure it has the concrete “回到主线” targets below:

- `01-neuron-mlp-activation.md` should point learners back to `../../04-transformer-foundations/05-transformer-block.md`, because linear layers, MLP, and activation functions are needed when reading Transformer Block and FFN.
- `02-backprop-gradient-problems.md` should point learners back to `../../04-transformer-foundations/05-transformer-block.md`.
- `03-normalization-residual-initialization.md` should point learners back to `../../04-transformer-foundations/05-transformer-block.md`.
- `04-ffn-gating-for-transformer.md` should point learners back to `../../04-transformer-foundations/05-transformer-block.md`.

If a page already has the exact target above, leave that return link unchanged.

- [ ] **Step 3: Add old deep-learning reference links if missing**

For each existing DL foundation page, ensure the old-reference link below appears near the end before the final return link:

```markdown
深入参考可回到 [旧版深度学习基础](../../02-deep-learning/)。
```

Do not rewrite the page body beyond adding or correcting the return/deep-reference links above.

- [ ] **Step 4: Verify and commit DL integration**

Run:

```bash
rg -n "Foundations 解锁层|旧版深度学习基础|04-transformer-foundations/05-transformer-block|回到主线" ai/ml-to-llm-roadmap/foundations/deep-learning
git diff --check
```

Commit:

```bash
git add ai/ml-to-llm-roadmap/foundations/deep-learning
git commit -m "docs: integrate deep learning foundations"
```

---

## Task 5: Update Transformer Mainline Prerequisite Links

**Files:**

- Modify: `ai/ml-to-llm-roadmap/04-transformer-foundations/README.md`
- Modify: `ai/ml-to-llm-roadmap/04-transformer-foundations/02-token-to-vector.md`
- Modify: `ai/ml-to-llm-roadmap/04-transformer-foundations/03-why-attention-needs-context.md`
- Modify: `ai/ml-to-llm-roadmap/04-transformer-foundations/04-self-attention-qkv.md`
- Modify: `ai/ml-to-llm-roadmap/04-transformer-foundations/05-transformer-block.md`

- [ ] **Step 1: Update Transformer README 学前检查**

Modify `ai/ml-to-llm-roadmap/04-transformer-foundations/README.md`.

Replace the current `## 学前检查` table with a table that prioritizes foundations:

```markdown
| 概念 | 先补 |
|------|------|
| token、vocab、token id | [Token、Vocab 与 Token ID](../foundations/nlp-tokenization-embedding/01-token-vocab-token-id.md) |
| embedding table、位置信息 | [Embedding Table 与位置信息](../foundations/nlp-tokenization-embedding/02-embedding-table-and-position.md) |
| 向量、矩阵、点积 | [向量、矩阵和点积](../foundations/math-for-transformer/01-vector-matrix-dot-product.md) |
| logits、softmax、概率分布 | [Logits、Softmax 与概率分布](../foundations/math-for-transformer/02-logits-softmax-probability.md) |
| 神经元、线性层、激活函数 | [Deep Learning 补课 01](../foundations/deep-learning/01-neuron-mlp-activation.md) |
| 反向传播、梯度消失/爆炸 | [Deep Learning 补课 02](../foundations/deep-learning/02-backprop-gradient-problems.md) |
| Residual、LayerNorm、初始化 | [Deep Learning 补课 03](../foundations/deep-learning/03-normalization-residual-initialization.md) |
| FFN、GELU、SwiGLU | [Deep Learning 补课 04](../foundations/deep-learning/04-ffn-gating-for-transformer.md) |
```

Add one sentence after the table:

```markdown
旧 `00-03` 目录是解锁后的深入资料；第一次卡住时优先读 `foundations/` 对应短文。
```

- [ ] **Step 2: Update `02-token-to-vector.md` prerequisites**

Modify the `## 学前检查` section:

- Add link to `../foundations/nlp-tokenization-embedding/01-token-vocab-token-id.md` for token/vocab/token id.
- Add link to `../foundations/nlp-tokenization-embedding/02-embedding-table-and-position.md` for embedding table and position.
- Replace old direct linear algebra link with `../foundations/math-for-transformer/01-vector-matrix-dot-product.md`.
- Put old math references only in a final “深入参考” sentence if the page already has such a section; otherwise rely on the math foundation pages to link old math.

- [ ] **Step 3: Update `03-why-attention-needs-context.md` prerequisites**

Modify the `## 学前检查` section:

- Keep the existing link to `02-token-to-vector.md`.
- Add a fallback link for token/embedding confusion:

```markdown
- 如果 token、token id、embedding table 仍不清楚，先补 [Tokenization 与 Embedding 补课](../foundations/nlp-tokenization-embedding/)。
```

- [ ] **Step 4: Update `04-self-attention-qkv.md` prerequisites**

Modify the `## 学前检查` section:

- Replace direct old probability link with `../foundations/math-for-transformer/02-logits-softmax-probability.md`.
- Add link to `../foundations/math-for-transformer/01-vector-matrix-dot-product.md` for `QK^T` and dot product.
- Add link to `../foundations/math-for-transformer/03-attention-math-minimal.md` for the full formula.
- Old `00-math-foundations` pages should remain deep references only through the math foundation pages.

- [ ] **Step 5: Update `05-transformer-block.md` prerequisites**

Modify the `## 学前检查` section:

- Keep links to existing deep-learning foundation pages.
- Add a link to `../foundations/README.md` in a short sentence:

```markdown
如果你不确定自己卡在数学、token 还是神经网络基础，先看 [Foundations 解锁层](../foundations/)。
```

- [ ] **Step 6: Verify and commit Transformer navigation**

Run:

```bash
rg -n "foundations/math-for-transformer|foundations/nlp-tokenization-embedding|Foundations 解锁层|旧 `00-03`|00-math-foundations|03-nlp-embedding-retrieval" ai/ml-to-llm-roadmap/04-transformer-foundations/README.md ai/ml-to-llm-roadmap/04-transformer-foundations/02-token-to-vector.md ai/ml-to-llm-roadmap/04-transformer-foundations/03-why-attention-needs-context.md ai/ml-to-llm-roadmap/04-transformer-foundations/04-self-attention-qkv.md ai/ml-to-llm-roadmap/04-transformer-foundations/05-transformer-block.md
git diff --check
```

Expected:

- New foundation links are present.
- Direct old `00`/`03` prerequisite links are no longer the first recovery path.

Commit:

```bash
git add ai/ml-to-llm-roadmap/04-transformer-foundations/README.md ai/ml-to-llm-roadmap/04-transformer-foundations/02-token-to-vector.md ai/ml-to-llm-roadmap/04-transformer-foundations/03-why-attention-needs-context.md ai/ml-to-llm-roadmap/04-transformer-foundations/04-self-attention-qkv.md ai/ml-to-llm-roadmap/04-transformer-foundations/05-transformer-block.md
git commit -m "docs: route transformer prerequisites through foundations"
```

---

## Task 6: Update Root Roadmap And Old 00-03 README Navigation

**Files:**

- Modify: `ai/ml-to-llm-roadmap.md`
- Modify: `ai/ml-to-llm-roadmap/00-math-foundations/README.md`
- Modify: `ai/ml-to-llm-roadmap/01-ml-basics/README.md`
- Modify: `ai/ml-to-llm-roadmap/02-deep-learning/README.md`
- Modify: `ai/ml-to-llm-roadmap/03-nlp-embedding-retrieval/README.md`

- [ ] **Step 1: Update root roadmap migration table**

Modify `ai/ml-to-llm-roadmap.md`.

Change the `Deep Learning 补课` row into a broader foundations row:

```markdown
| Foundations 解锁层 | 支撑 Transformer 的数学、NLP 与神经网络最小前置 | [foundations](./ml-to-llm-roadmap/foundations/) | 已系统化 |
```

Do not remove existing mainline module rows.

- [ ] **Step 2: Update root roadmap system learning path**

Modify `## 系统学习路径` so it says:

```markdown
1. 从 [Transformer 必要基础](./ml-to-llm-roadmap/04-transformer-foundations/) 开始。
2. 卡在数学、NLP/token/embedding 或 Deep Learning 前置知识时，进入 [foundations 解锁层](./ml-to-llm-roadmap/foundations/)。
3. 旧 `00-03` 是解锁后的深入资料，不再作为第一默认路线。
4. 如果目标是应用系统，按需进入：RAG、Agent、生成控制、推理部署、评估安全、系统设计。
5. 如果目标是模型理解，按需进入：训练对齐、推理部署、评估安全。
6. 每个模块学完后，用对应 `09-review-notes/` 做面试复盘。
```

- [ ] **Step 3: Update `00-math-foundations/README.md`**

Add a new route notice after the title:

```markdown
> **新版路线说明**：这个目录是解锁后的数学深入资料，不是默认主线。如果你是从 Transformer 主线卡在向量、矩阵、点积、logits 或 softmax，先读 [Transformer 最小数学](../foundations/math-for-transformer/)；读完再回到这里补完整数学直觉。
```

Add a section before resources:

```markdown
## 什么时候读旧数学资料

| 你已经读懂 | 再回来看 |
|------------|----------|
| 向量、矩阵、点积的最小直觉 | [线性代数](./01-linear-algebra.md) |
| logits、softmax、概率分布的最小直觉 | [概率与统计](./02-probability.md) |
| 反向传播为什么需要梯度 | [微积分](./03-calculus.md) |
| 交叉熵、KL、信息量在训练和对齐中的意义 | [信息论](./04-information-theory.md) |
```

- [ ] **Step 4: Update `01-ml-basics/README.md`**

Add a new route notice after the title:

```markdown
> **新版路线说明**：这个目录是解锁后的传统 ML 深入资料，不是进入 LLM 主线的硬性前置。第一次读 Transformer 时不需要先完整学习本目录；当你想补“模型如何学习、评估、泛化”时再回来。
```

Add a section before resources:

```markdown
## 什么时候回来看 ML 基础

| 你想补强 | 再回来看 |
|----------|----------|
| 模型、特征、损失、优化、训练/测试划分这些共同语言 | [核心框架](./01-core-concepts.md) |
| 传统模型如何从人工特征走向学习表示 | [经典算法](./02-classic-algorithms.md) |
| 评估、调参、过拟合、泛化这些工程判断 | [模型评估 & 调优](./03-evaluation-tuning.md) |
```

- [ ] **Step 5: Update `02-deep-learning/README.md`**

Modify the existing new-route section so the default path points to root foundations:

- Add a default entry:

```markdown
- [Foundations 解锁层](../foundations/)
```

- Keep `Deep Learning 补课` linking to `../foundations/deep-learning/` as the specific DL entry.
- Ensure wording says old `02-deep-learning` is deep reference after foundation pages.

- [ ] **Step 6: Update `03-nlp-embedding-retrieval/README.md`**

Modify the route notice so it also points to the new NLP foundation:

```markdown
如果你是从 Transformer 主线卡在 token、vocab、token id 或 embedding table，先读 [Tokenization 与 Embedding 补课](../foundations/nlp-tokenization-embedding/)；读完再回到这里补完整 NLP、Embedding 与检索理论。
```

Add a section before the learning path guide:

```markdown
## 什么时候读旧 NLP/Embedding 资料

| 你已经读懂 | 再回来看 |
|------------|----------|
| token、vocab、token id、embedding table | [文本表示演进](./01-text-representation.md) 和 [Embedding 深度理论](./02-embedding-theory.md) |
| RAG 主线里的召回、rerank、context assembly | [检索理论](./03-retrieval-theory.md) |
| 生成控制主线 | [语言模型 & 解码旧版参考](./04-language-model-decoding.md) |
| 需要 BPE 算法细节 | [Tokenization 算法](./06-tokenization-deep-dive.md) |
```

- [ ] **Step 7: Verify and commit route navigation**

Run:

```bash
rg -n "Foundations 解锁层|Transformer 最小数学|Tokenization 与 Embedding 补课|解锁后的|默认主线|什么时候读|什么时候回来看|foundations" ai/ml-to-llm-roadmap.md ai/ml-to-llm-roadmap/00-math-foundations/README.md ai/ml-to-llm-roadmap/01-ml-basics/README.md ai/ml-to-llm-roadmap/02-deep-learning/README.md ai/ml-to-llm-roadmap/03-nlp-embedding-retrieval/README.md
git diff --check
```

Commit:

```bash
git add ai/ml-to-llm-roadmap.md ai/ml-to-llm-roadmap/00-math-foundations/README.md ai/ml-to-llm-roadmap/01-ml-basics/README.md ai/ml-to-llm-roadmap/02-deep-learning/README.md ai/ml-to-llm-roadmap/03-nlp-embedding-retrieval/README.md
git commit -m "docs: update 00-03 unlock navigation"
```

---

## Task 7: Final Validation

**Files:**

- Validate all changed files in this branch.

- [ ] **Step 1: Run whitespace check**

Run:

```bash
git diff --check main..HEAD
```

Expected:

- No output.

- [ ] **Step 2: Run local Markdown link checker**

Run:

```bash
python3 - <<'PY'
from pathlib import Path
import re
import subprocess

base = 'main'
changed = subprocess.check_output(['git', 'diff', '--name-only', f'{base}..HEAD', '--', '*.md'], text=True).splitlines()
files = [Path(p) for p in changed if Path(p).is_file()]
link_re = re.compile(r'(?<!!)(?:\[[^\]]*\]|\[\[[^\]]*\]\])\(([^)]+)\)')
errors = []
for file in files:
    in_code = False
    for line_no, line in enumerate(file.read_text().splitlines(), 1):
        if line.strip().startswith('```'):
            in_code = not in_code
            continue
        if in_code:
            continue
        for raw in link_re.findall(line):
            target = raw.split()[0].strip('<>')
            if not target or target.startswith(('#', 'http://', 'https://', 'mailto:')):
                continue
            path_part = target.split('#', 1)[0]
            if not path_part:
                continue
            dest = (file.parent / path_part).resolve()
            if not dest.exists():
                errors.append(f'{file}:{line_no}: missing link target {raw}')
if errors:
    print('\n'.join(errors))
    raise SystemExit(1)
print(f'checked {len(files)} changed markdown files: all local links resolve')
PY
```

Expected:

- Link checker prints `all local links resolve`.

- [ ] **Step 3: Run new foundation structure checker**

Run:

```bash
python3 - <<'PY'
from pathlib import Path

required = [
    '## 这篇解决什么卡点',
    '## 先记住一句话',
    '## 最小例子',
    '## 这个概念在 Transformer 哪里出现',
    '## 和旧资料的连接',
    '## 自测',
    '## 回到主线',
]
files = [
    Path('ai/ml-to-llm-roadmap/foundations/math-for-transformer/01-vector-matrix-dot-product.md'),
    Path('ai/ml-to-llm-roadmap/foundations/math-for-transformer/02-logits-softmax-probability.md'),
    Path('ai/ml-to-llm-roadmap/foundations/math-for-transformer/03-attention-math-minimal.md'),
    Path('ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/01-token-vocab-token-id.md'),
    Path('ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/02-embedding-table-and-position.md'),
    Path('ai/ml-to-llm-roadmap/foundations/nlp-tokenization-embedding/03-from-text-representation-to-transformer.md'),
]
errors = []
for file in files:
    text = file.read_text()
    for heading in required:
        if heading not in text:
            errors.append(f'{file}: missing {heading}')
if errors:
    print('\n'.join(errors))
    raise SystemExit(1)
print('new foundation pages have required headings')
PY
```

Expected:

- Structure checker prints required heading success.

- [ ] **Step 4: Run navigation status checks**

Run:

```bash
rg -n "foundations/math-for-transformer|foundations/nlp-tokenization-embedding|Foundations 解锁层|Transformer 最小数学|Tokenization 与 Embedding 补课|解锁后的深入资料|旧 `00-03`" ai/ml-to-llm-roadmap.md ai/ml-to-llm-roadmap/foundations ai/ml-to-llm-roadmap/04-transformer-foundations ai/ml-to-llm-roadmap/00-math-foundations/README.md ai/ml-to-llm-roadmap/01-ml-basics/README.md ai/ml-to-llm-roadmap/02-deep-learning/README.md ai/ml-to-llm-roadmap/03-nlp-embedding-retrieval/README.md
```

Expected:

- New foundation links are present.
- Old `00-03` README files are marked as deep references, not the first default route.

- [ ] **Step 5: Final read-through**

Check:

- `foundations/README.md` gives a learner a clear “卡在哪里 -> 读哪篇” decision table.
- Math foundation pages bridge to Transformer math without becoming a full math course.
- NLP foundation pages bridge token/embedding concepts without becoming a full NLP course.
- Transformer `README.md` and pages `02-05` use foundations as first recovery links.
- Old `00-03` README files preserve old material but clearly defer default learners to foundations first.
