# 金融级分布式事务场景矩阵扩展设计

日期：2026-05-01

## 目标

扩展 `financial-consistency/00-scenario-matrix.md`，让它不仅覆盖第一批常见场景，还覆盖金融、支付、清结算、平台交易和国际业务中更容易被忽略但非常关键的分布式事务场景。

这次扩展不把每个场景写成完整教程。它只做两件事：

1. 补充高价值场景，让场景覆盖更贴近真实生产系统。
2. 增加“场景卡最小深度”标准，为后续逐个深挖模块提供统一模板。

## 当前状态

现有矩阵已经覆盖 14 类场景：

- 账户转账。
- 充值、提现、支付回调。
- 电商下单、支付、库存扣减。
- 订单取消、退款、组合支付。
- 机票、酒店、租车组合预订。
- 外部渠道超时或未知状态。
- 跨境汇款、多币种结算。
- 对账发现单边账。
- 人工补偿与运营修复。

这些内容适合作为第一版全局地图，但仍有两个不足：

- 覆盖面缺少预授权、分账、拒付、订阅、银行代扣、批量清算、贷款还款等真实金融场景。
- 每个场景目前只有一行矩阵，足够做导航，不足以指导后续完整教程拆解。

## 扩展方式

采用小步扩展，而不是一次性把矩阵膨胀成大百科。

新增一个章节：

```markdown
## 待补充高价值场景
```

这个章节放在现有“场景矩阵”之后、“模式选择规则”之前。它说明这些场景暂时不进入主矩阵的第一学习顺序，但必须进入后续路线规划。

新增另一个章节：

```markdown
## 场景卡最小深度
```

这个章节放在“模式选择规则”之后、“进入具体模块的顺序”之前。它规定后续任何场景从矩阵进入完整模块时，至少要回答哪些问题。

## 新增高价值场景

第一轮补充 7 类场景。

### 1. 预授权、授权占用、Capture、Void

代表业务：

- 酒店押金。
- 租车押金。
- 信用卡担保交易。
- 先授权占用额度，后确认扣款或撤销授权。

为什么重要：

- 它不是普通扣款。
- 授权占用、确认扣款和撤销授权是不同事实。
- 授权可能过期，Capture 可能部分成功，Void 可能晚到或失败。

后续归属：

- `02-payment-recharge-withdraw`
- `04-travel-booking-saga`

### 2. 商户结算、分账、平台佣金

代表业务：

- 电商平台把订单收入拆给商户、平台佣金、服务费、税费和渠道手续费。
- 支付平台按日或按周期给商户结算。

为什么重要：

- 钱不是简单从 A 到 B。
- 一笔交易会产生多方应收应付。
- 退款、拒付、补贴、手续费退还会影响后续结算。

后续归属：

- `03-order-payment-inventory`
- `07-reconciliation`

### 3. 拒付、争议、Chargeback

代表业务：

- 持卡人对已成功交易发起争议。
- 卡组织或银行把款项从商户侧冲回。
- 商户提交举证材料后可能胜诉或败诉。

为什么重要：

- 成功交易很久以后仍可能反向变化。
- 它不是普通退款，因为发起方和裁决方不完全由平台控制。
- 需要证据链、冻结准备金、商户账务调整和对账。

后续归属：

- `07-reconciliation`
- `08-interview-synthesis`

### 4. 订阅扣费、自动续费、失败重试

代表业务：

- 月度会员。
- SaaS 订阅。
- 自动续费扣款失败后的重试、宽限期和权益暂停。

为什么重要：

- 它是长期状态机，不是单次交易。
- 扣款失败不一定立即终止服务。
- 重试策略、权益状态和账单状态必须一致。

后续归属：

- `02-payment-recharge-withdraw`
- `03-order-payment-inventory`

### 5. ACH、Direct Debit、银行代扣

代表业务：

- 用户授权从银行账户扣款。
- 扣款请求提交后，几天后才知道最终成功或失败。
- 成功后仍可能退回。

为什么重要：

- 这类系统不是实时支付。
- “提交成功”不等于“资金成功”。
- 需要 pending 状态、退回处理、账期和渠道对账。

后续归属：

- `02-payment-recharge-withdraw`
- `07-reconciliation`

### 6. 商户日终清算、批量出款

代表业务：

- 平台按日汇总商户可结算金额。
- 生成清算批次和出款文件。
- 银行返回部分成功、部分失败。

为什么重要：

- 从在线单笔交易进入批处理。
- 批次、明细、出款结果和账本必须能互相核对。
- 部分失败不能影响已成功明细，也不能重复出款。

后续归属：

- `07-reconciliation`

### 7. 贷款还款、分期、利息计提

代表业务：

- 贷款分期还款。
- 逾期罚息。
- 提前还款。
- 自动扣款失败后的补扣。

为什么重要：

- 引入账期、应收、利息、罚息和摊销。
- 还款顺序会影响本金、利息和费用。
- 冲正和调整必须保留历史证据。

后续归属：

- `07-reconciliation`
- 独立后续模块 `09-credit-and-loan`

## 场景卡最小深度

后续任何场景从矩阵进入完整模块时，至少要有一张“场景卡”。场景卡不是代码设计，而是正式建模前的业务和一致性边界说明。

每张场景卡必须包含：

| 字段 | 说明 |
| --- | --- |
| 参与方 | 用户、平台、账户服务、账本服务、支付渠道、银行、商户、供应商、运营人员等 |
| 核心状态机 | 关键业务状态，以及哪些状态不能回退 |
| 耐久事实 | 哪些记录、事件、分录或渠道结果一旦写入就代表业务事实 |
| 最危险失败点 | 超时、重复、乱序、部分成功、外部未知、人工误操作等 |
| 补偿方式 | 释放、冲正、退款、退汇、赔付、补事件、人工处理等 |
| 对账来源 | 本地订单、账户流水、会计分录、Outbox、消费记录、渠道账单、供应商账单 |
| 验证方式 | 不变量、属性测试、集成测试、故障注入、历史检查、对账规则 |

这个标准用于控制后续教程深度：不允许只讲框架，也不允许只讲 happy path。

## README 更新

`financial-consistency/README.md` 需要加入本设计文档链接：

```markdown
- [2026-05-01-financial-scenario-matrix-expansion-design.md](../docs/superpowers/specs/2026-05-01-financial-scenario-matrix-expansion-design.md)
```

暂时不新增 `09-credit-and-loan` 到主阶段路线。贷款模块只作为远期可能模块出现在矩阵扩展说明中。等 `02` 到 `07` 主线完成后，再决定是否纳入主路线。

## 非目标

这次不创建 `02-payment-recharge-withdraw`。

这次不展开新增 7 类场景的完整状态机、事件流、失败矩阵或验证路线。

这次不改已有 `01-transfer` 模块。

这次不引入代码实现。

## 验收标准

完成后应满足：

- `financial-consistency/00-scenario-matrix.md` 新增“待补充高价值场景”章节。
- 新章节覆盖预授权、商户结算、拒付、订阅、ACH/Direct Debit、商户日终清算、贷款还款 7 类场景。
- `financial-consistency/00-scenario-matrix.md` 新增“场景卡最小深度”章节。
- 场景卡最小深度包含参与方、核心状态机、耐久事实、最危险失败点、补偿方式、对账来源、验证方式。
- `financial-consistency/README.md` 链接本设计文档。
- 不修改 `01-transfer` 下的既有文档。
- 文档完整、无含糊要求、无空白格式问题。

## 后续工作

设计确认后，进入实施计划：

1. 更新 `financial-consistency/00-scenario-matrix.md`。
2. 更新 `financial-consistency/README.md`。
3. 验证新增场景和场景卡字段均可搜索。
4. 检查未完成标记和 Markdown 空白格式。
5. 提交文档变更。
