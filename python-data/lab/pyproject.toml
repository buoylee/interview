[project]
name = "python-data-lab"
version = "0.1.0"
description = "Runnable Postgres lab for the python-data tutorial"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy[asyncio]>=2.0",
    "psycopg[binary]>=3.1",
    "asyncpg>=0.29",
    "alembic>=1.13",
]
