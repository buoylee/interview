# 为什么需要

操作完 vue 对象, 还需要实现响应式逻辑, 来渲染dom, 

但是为了性能和效率, 不可能在执行一个很长的方法时, 如果不停触发重新渲染, 就一直更新dom, 会有性能问题.

只有当执行一个方法结束时, 才会延迟触发渲染,





例子: 当 vue.xxx_input.visable = true 时, dom 不会马上出现, 这个时候,如果 ref.xxx_input.focus = true 是不会成功, 因为 dom 没有实际渲染回页面中.



