# width/height

也可以用%, 继承于父元素





# vw(viewpoint width 的百分比)

50vw: 50%视口

# vh(viewpoint height 的百分比)



# vmax(vw和vw中比较大的那个 的百分比)



# vmin(同理vmax)



# rem(root em(本质是表示 m字母的宽, element measure来记忆)根元素字体大小的倍数)



# em(同理上, 当前元素font-size(字体大小)的倍数)

font-size 也可以用 em表示, 就会去继承父元素, 如果还是没有, 会一直往上找



