worker_processes auto;

events {
    worker_connections 1024;
}

http {
    # keepalive 讓持久連線在 reload 中仍被優雅處理
    keepalive_timeout 65;
    keepalive_requests 1000;

    server {
        listen 80;

        location / {
            return 200 "ok\n";
        }
    }
}
