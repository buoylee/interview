```
  我建议的顺序（fastapi 为脊柱，另两个当补给）

  Phase 0 — 今天就让自己看到真实数字（消除不安）
  跑 concurrency-capacity/lab/service：docker compose up → python drive.py →
  看它在压力下饱和。配
  experiments/e04-find-saturation、e08-read-the-signals。半天，纯粹为了当天就观察到真
  东西，证明这套闭环在你笔电上跑得起来。这是你的"逃生票"——不安立刻消失。

  主线（fastapi-ops 贯穿始终，按阶段抽 performance-tuning-roadmap 当即时底座）

  1. fastapi-ops 阶段一 (01) → 亲手把那个 FastAPI
  模板建出来。这是你之后所有观测的"被观测对象"。
  2. fastapi-ops 阶段二 (02→03→04→05) ＋ performance-tuning-roadmap/03-observability
  （metrics-theory、prometheus-grafana、tracing 当原理底座）＋
  03x-load-gen-quickstart（locust/wrk
  打流量）。到这一步你就完全进入"能观察"状态——而且是你自己的 fastapi 服务在动。
  3. fastapi-ops 阶段三 (07 压测 → 06 profiling) ＋
  performance-tuning-roadmap/06a-python-profiling（语言级剖析底座）。
  4. fastapi-ops 阶段四 (08/09 调优) ＋ performance-tuning-roadmap/01-methodology + 
  02-linux-tools（USE/RED 方法论、linux
  工具——调优时回头补，别前置，否则又变成读理论）。

  收尾

  5. concurrency-capacity 全篇：放最后。此时 Little 定律、定容公式讲的全是你在阶段二
  三已经亲眼看过的现象（队列涨、延迟拐点、饱和点），数学自然落地，不再是悬空公式。

  一句话总结

  - fastapi-ops = 脊柱，但要 build-along 不是读（它没代码）。
  - performance-tuning-roadmap = 即时底座，别整本前置，按 fastapi
  当前阶段抽对应章（观测→03+03x；剖析→06a；调优→01+02）。
  - concurrency-capacity = ①Phase 0
  的"立刻能观察"沙盒（它唯一现成可跑），②最后的理论收尾。

  要不要我先陪你把 Phase 0 跑通——concurrency-capacity/lab/service 起来、drive.py
  打上去、看着它饱和？那一刻你那个"不放心"基本就没了，后面 fastapi-ops 的 build-along
  你也会更敢动手。
```

