# 05 · Consumer (rebalance / assignor / offset / lag)

> 本章節尚未開始。規劃 scenarios:rebalance 全過程(stop-the-world 觀察)、cooperative sticky vs range、offset commit 時機 vs 重複消費、lag 突增定位。

掃 `00-lab/` 起 lab,然後從 `02-replication/` 開始(第一個有完整 scenario 的章節)。
