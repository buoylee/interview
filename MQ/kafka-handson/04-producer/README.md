# 04 · Producer (acks / idempotent / batch / linger)

> 本章節尚未開始。規劃 scenarios:acks=0/1/all 的丟失差異、idempotent producer 重試、batch + linger 對吞吐的影響、partitioner 選擇。

掃 `00-lab/` 起 lab,然後從 `02-replication/` 開始(第一個有完整 scenario 的章節)。
