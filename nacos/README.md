# nacos/ — 舊組件筆記(已被吸收)

> ⚠️ 這個目錄是早期對 Nacos(AP/CP、配置中心、與 ZooKeeper 區別)的零散筆記,**當詞典查**即可。
>
> **「Nacos 在 k8s 時代還要不要留、什麼時候留、和 CoreDNS 的特性差怎麼補」這類選型敘事,已搬到專題 track:**
>
> 👉 **`service-governance-on-k8s/`**
> - `02` — 同層選一(雙真相源)+ 特性差補法矩陣(CoreDNS 缺權重 / 推送 / 灰度時,何時「保留富 registry = 留 Nacos」)
> - Nacos 仍是國內 Java 主流(發現 + 配置一體,Spring Cloud Alibaba 跟到 Boot 4.x)——這個結論在該 track 的選型表裡。
>
> 這裡的 stub 不再更新;新內容以該 track 為準。
