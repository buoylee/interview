# 0.2 概率与统计（Day 3-4）

> **一句话定位**：语言模型本质是概率模型 P(下一个词|前面的词)。理解概率是理解 ML 几乎所有算法的基础。

---

## 目录

- [1. 基础概念](#1-基础概念)
- [2. 条件概率与贝叶斯](#2-条件概率与贝叶斯)
- [3. 常见概率分布](#3-常见概率分布)
- [4. 期望、方差、协方差](#4-期望方差协方差)
- [5. MLE vs MAP](#5-mle-vs-map)
- [6. 在 ML/LLM 中的应用](#6-在-mllm-中的应用)
- [7. 面试常问](#7-面试常问)

---

## 1. 基础概念

### 1.1 概率是什么？

**直觉**：一件事发生的可能性大小，值域 [0, 1]。

```
P(A) = 事件 A 发生的可能性
P(A) = 0 → 不可能
P(A) = 1 → 必然发生
```

### 1.2 联合概率 vs 边际概率

```
联合概率:  P(A, B)  = A 和 B 同时发生
边际概率:  P(A)     = 不管 B 怎样，A 发生的总概率
          P(A) = Σ P(A, B=bᵢ)  （对 B 所有可能求和）
```

### 1.3 独立性

```
如果 A 和 B 独立:  P(A, B) = P(A) × P(B)
```

**直觉**：A 发生不影响 B 的概率。

> 🔑 **ML 关联**：朴素贝叶斯假设所有特征独立（虽然现实中很少成立，但有时效果出奇好）。

---

## 2. 条件概率与贝叶斯

### 2.1 条件概率

```
P(A|B) = P(A, B) / P(B)
```

**直觉**：已知 B 发生了，A 发生的概率。

> 🔑 **ML 关联**：**语言模型的核心就是条件概率！**
> ```
> P("猫" | "我家有一只可爱的") = ?
> GPT 做的就是：给定前面的词，预测下一个词的概率分布
> ```

### 2.2 贝叶斯定理 ⭐

```
P(A|B) = (P(B|A) × P(A)) / P(B)

后验 = (似然 × 先验) / 证据
```

**具体例子：你去做流感检测，测出了阳性，你真有流感的概率是多少？**

先定义 A 和 B：

```
A = 你真的有流感（事实上有没有病）
B = 测试结果显示阳性（试剂给你的结果）
```

已知信息：

```
P(A)   = 1%    ← 先验：流感发病率1%，没测之前你大概率没事
P(B|A) = 99%   ← 似然：如果你真有流感，测出阳性的概率（试剂很准）
误报率  = 5%   ← 如果你没流感，也有5%几率误测为阳性（试剂不完美）
```

想象 10000 人一起测：

```
10000 人
  ├── 100 人真有流感（1%）
  │     └── 其中 99 人测出阳性 ✅（99%准确率）
  │
  └── 9900 人没有流感（99%）
        └── 其中 495 人也测出阳性 ❌（5%误报）

测出阳性的总人数 P(B) = (99 + 495) / 10000 = 5.94%
其中真有流感的 = 99 人
```

代入公式：

```
P(A|B) = (P(B|A) × P(A)) / P(B)
       = (99% × 1%) / 5.94%
       = 16.7%

测出阳性后，你真有流感的概率只有 16.7%！
因为本来得流感的人就很少（1%），误报的人反而更多。
```

**一句话总结**：贝叶斯就是"拿到新证据（阳性结果）后，更新你原来的判断（从 1% 升到 16.7%）"。

**贝叶斯解决的核心问题**：你知道 P(B|A)（有病→测出阳性 = 99%），但你真正想知道的是反过来的 P(A|B)（测出阳性→真有病 = 只有 16.7%）。这两个概率完全不同！

### 2.3 贝叶斯在 ML 中的应用

| 场景 | 贝叶斯怎么用 |
|------|------------|
| 朴素贝叶斯分类 | P(类别\|特征) ∝ P(特征\|类别) × P(类别) |
| 正则化 | L2 正则化 ≈ 高斯先验（后面 MAP 会解释）|
| 贝叶斯优化 | 超参数搜索时建模目标函数 |
| LLM 生成 | 本质上在做 P(next_token \| context) |

---

## 3. 常见概率分布

### 3.1 离散分布

#### 伯努利分布 (Bernoulli)

```
只有两个结果：成功(1) 或 失败(0)
P(X=1) = p,  P(X=0) = 1-p
```

**例子**：抛一次硬币、二分类问题的输出。

#### 多项式分布 (Categorical / Multinomial)

```
多个类别中选一个
P(X=k) = pₖ,  Σpₖ = 1
```

> 🔑 **ML 关联**：LLM 每次生成 token 就是从一个多项式分布中采样！
> ```
> 词表大小 = 50000
> 每个词都有一个概率 → softmax 输出就是这个多项式分布
> ```

### 3.2 连续分布

#### 正态分布 / 高斯分布 (Normal / Gaussian) ⭐

```
N(μ, σ²)
μ = 均值（中心位置）
σ = 标准差（分散程度）
```

**为什么到处都是正态分布？**
- **中心极限定理**：大量独立随机变量的均值趋向正态分布
- 权重初始化通常用正态分布
- 很多自然现象近似正态

> 🔑 **ML 关联**：
> - 权重初始化：N(0, 1/√n)（Xavier/He 初始化）
> - VAE 的潜空间假设为正态分布
> - Dropout 也可理解为一种噪声注入

---

## 4. 期望、方差、协方差

### 4.1 期望（Expectation / Mean）

```
E[X] = Σ xᵢ × P(xᵢ)         (离散)
E[X] = ∫ x × f(x) dx        (连续)
```

**直觉**：「平均来看会怎样」，加权平均。

### 4.2 方差（Variance）

```
Var(X) = E[(X - μ)²] = E[X²] - (E[X])²
标准差 σ = √Var(X)
```

**直觉**：数据有多「分散」。方差大 = 不稳定。

> 🔑 **ML 关联**：
> - **偏差-方差权衡**：模型的误差 = 偏差² + 方差 + 噪声
> - Batch Normalization 就是让每层输出的均值=0、方差=1

### 4.3 协方差（Covariance）

```
Cov(X, Y) = E[(X - μ_X)(Y - μ_Y)]
```

**直觉**：两个变量一起变化的趋势。
- 正协方差 → 一起增大
- 负协方差 → 一个增大另一个减小
- 零协方差 → 没有线性关系

**协方差矩阵**：多个变量之间的协方差组成矩阵 → PCA 就是对协方差矩阵做特征分解。

---

## 5. MLE vs MAP

### 5.1 最大似然估计（MLE）⭐

```
θ_MLE = argmax P(数据 | θ)
```

**直觉**：找让观察到的数据「最可能出现」的参数。

**例子**：抛 10 次硬币，7 次正面 → MLE 估计 p = 0.7

> 🔑 **ML 关联**：大多数 ML 模型训练就是在做 MLE！
> - 最小化交叉熵损失 = 最大化似然
> - LLM 预训练的目标函数就是最大化 P(数据|模型参数)

### 5.2 最大后验估计（MAP）

```
θ_MAP = argmax P(θ | 数据) = argmax P(数据 | θ) × P(θ)

                              似然          先验
```

**直觉**：MLE + 先验知识。

- 如果先验是高斯 N(0, σ²) → **等价于 L2 正则化**
- 如果先验是拉普拉斯 → **等价于 L1 正则化**

> 🔑 **ML 关联**：正则化的概率解释！加正则项 = 加先验假设（参数不应该太大）。

### 5.3 MLE vs MAP 对比

| | MLE | MAP |
|--|-----|-----|
| 公式 | argmax P(D\|θ) | argmax P(D\|θ)P(θ) |
| 直觉 | 只看数据 | 数据 + 先验 |
| 数据少时 | 容易过拟合 | 先验帮助约束 |
| 数据多时 | 两者趋同 | 先验影响变小 |
| ML 对应 | 无正则训练 | L1/L2 正则化 |

---

## 6. 在 ML/LLM 中的应用

| 概率概念 | ML/LLM 应用 | 阶段 |
|---------|------------|------|
| 条件概率 P(A\|B) | 语言模型 P(next\|context) | 3, 5, 6 |
| 贝叶斯定理 | 朴素贝叶斯、正则化的概率解释 | 1 |
| 多项式分布 | softmax 输出、token 采样 | 3, 4 |
| 正态分布 | 权重初始化、VAE | 2 |
| 期望 | 损失函数 = 期望损失 | 1, 2 |
| 方差 | 偏差-方差权衡、BN | 1, 2 |
| MLE | 模型训练目标 | 1, 6 |
| MAP | 正则化 = 先验 | 1, 6 |

---

## 7. 面试常问

### Q1: 为什么语言模型用交叉熵损失？

**答**：
- 语言模型的目标是最大化 P(正确 token | context)
- 最大化似然 = 最小化负对数似然 = 最小化交叉熵
- 交叉熵 H(p, q) = -Σ p(x) log q(x)
- 当 p 是 one-hot（真实标签），q 是模型预测时，这就等于 -log q(正确 token)
- 直觉：让模型给正确答案分配尽可能高的概率

### Q2: softmax 是什么？为什么用它？

**答**：
```
softmax(zᵢ) = e^zᵢ / Σⱼ e^zⱼ
```
- 把任意实数向量变成概率分布（所有值 > 0，和 = 1）
- 保持原来的大小关系（更大的 logit → 更高的概率）
- **Temperature 控制**：softmax(z/T)
  - T > 1 → 分布更均匀（更随机）
  - T < 1 → 分布更尖锐（更确定）
  - T → 0 → 等价于 argmax（贪婪）

> 🔑 这就是 LLM 中 Temperature 参数的数学原理！

### Q3: MLE 和 MAP 的区别？什么时候用哪个？

**答**：见上文 5.3 的对比表。简短版：
- 数据充足 → MLE 就够
- 数据少或容易过拟合 → 用 MAP（加正则化）
- 实际中：几乎所有训练都加了正则化，所以本质上都是 MAP

### Q4: 什么是采样 (Sampling) vs 贪婪解码 (Greedy Decoding)？

**答**：
- **贪婪**：每步选概率最高的 token → 确定性，但无聊重复
- **采样**：按概率分布随机选 → 多样性，但可能不连贯
- **Top-k**：只从前 k 个最高概率中采样
- **Top-p (Nucleus)**：只从累积概率 ≥ p 的最小集合中采样
- 这些都是在多项式分布上的不同采样策略

---

## 📖 推荐学习路径

1. **StatQuest 的概率统计系列**（YouTube，每集 10-15 分钟）
2. 重点理解：条件概率、贝叶斯、正态分布、MLE
3. 确保理解 softmax 和 Temperature → 这在 LLM 中直接使用

> ⬅️ [上一节：线性代数](./01-linear-algebra.md) | [返回概览](./README.md) | ➡️ [下一节：微积分](./03-calculus.md)
