```
pg_dump -h 127.0.0.1 -U postgres -d pgvector_dev \
  -t langchain_pg_collection \
  -t langchain_pg_embedding \
  --schema-only \
  > langchain_schema.sql
```





```
pg_dump -h 127.0.0.1 -U postgres -d pgvector_dev \
  -t langchain_pg_collection \
  -t langchain_pg_embedding \
  --data-only \
  > langchain_data.sql
```

